<UserControl x:Class="ShoppingMall.Client.Views.InventoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="10" Background="#1A56DB" CornerRadius="6" Margin="0,0,0,10">
            <TextBlock Text="Inventory Management" FontSize="20" FontWeight="Bold" Foreground="White"/>
        </Border>

        <Border Grid.Row="1" Padding="10" Background="White" CornerRadius="6" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Stock Level" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox Grid.Column="1" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         FontSize="14" Padding="8" BorderBrush="#D1D5DB" BorderThickness="1"/>
                <Button Grid.Column="2" Content="Refresh" Command="{Binding RefreshCommand}"
                        Background="#059669" Foreground="White" Padding="15,8" BorderThickness="0" Margin="10,0,0,0"/>
            </Grid>
        </Border>

        <ListView Grid.Row="2" ItemsSource="{Binding StockItems}" Background="White">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Product" Width="200" DisplayMemberBinding="{Binding ProductName}"/>
                    <GridViewColumn Header="SKU" Width="120" DisplayMemberBinding="{Binding SKU}"/>
                    <GridViewColumn Header="On Hand" Width="100" DisplayMemberBinding="{Binding OnHand}"/>
                    <GridViewColumn Header="Reserved" Width="100" DisplayMemberBinding="{Binding Reserved}"/>
                    <GridViewColumn Header="Available" Width="100" DisplayMemberBinding="{Binding Available}"/>
                    <GridViewColumn Header="Status" Width="120">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding StockStatus}" FontWeight="Bold"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
